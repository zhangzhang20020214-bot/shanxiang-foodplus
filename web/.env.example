# 复制为 .env.local 后填入真实值。.env.local 已在 .gitignore 中，不会被提交。
#
# ⚠️ 这两个变量**刻意不带 VITE_ 前缀**。
#    Vite 会把所有 VITE_ 变量原样编译进浏览器包，部署后任何人打开 F12
#    搜 `app-` 就能拿到密钥。所以密钥只允许存在于：
#      本地 → 本文件（vite.config.ts 的 server.proxy 读取并注入请求头）
#      线上 → 托管平台的 Environment Variables（api/dify 函数读取并注入请求头）
#    浏览器侧自始至终拿不到它。

# Dify 服务地址（不带结尾斜杠）
DIFY_BASEURL=https://api.dify.ai/v1

# Dify 应用 API key，形如 app-xxxxxxxxxxxxxxxx
DIFY_APIKEY=app-替换成你自己的

# ── 可选：纯静态托管时使用 ──────────────────────────────
# 没有服务端函数的环境（如 GitHub Pages）把下面这行加到 .env.local，
# 应用会走 src/mock/mockDify.ts 的本地假数据，不发起任何网络请求。
# 注意它带 VITE_ 前缀是故意的：这不是密钥，需要被编译进前端。
# VITE_USE_MOCK=true
